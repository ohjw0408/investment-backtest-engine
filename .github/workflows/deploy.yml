name: Deploy to Hetzner

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Hetzner VPS
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: 178.105.84.213
          username: root
          key: ${{ secrets.HETZNER_SSH_KEY }}
          script: |
            set -e
            cd /root/investment-backtest-engine
            # 런타임이 쓰는 추적-이력 DB가 dirty면 git pull이 abort → 배포 무력화됨.
            # pull 전 해당 파일 로컬변경 폐기(이후 .gitignore로 추적 안 됨). 그 뒤 pull 실패는 즉시 에러.
            git checkout -- data/meta/index_master.db 2>/dev/null || true
            git pull origin main
            cp deploy/domino-celery-beat.service /etc/systemd/system/
            systemctl daemon-reload
            systemctl enable domino-celery-beat
            systemctl restart domino domino-celery domino-celery-beat
            sleep 2
            systemctl is-active domino domino-celery domino-celery-beat
